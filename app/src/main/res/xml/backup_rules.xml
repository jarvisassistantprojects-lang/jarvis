<?xml version="1.0" encoding="utf-8"?>
<!-- Secrets are Keystore-encrypted but the encrypted blob still should not roam via backup. -->
<full-backup-content>
    <exclude domain="sharedpref" path="jarvis_secrets.xml" />
</full-backup-content>
